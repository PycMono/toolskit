

# Block J-07 · 数据格式转换 Part1（JSON↔CSV / JSON↔YAML / JSON↔XML）

> **路由**：`/json/to-csv` · `/json/from-csv` · `/json/to-yaml` · `/json/from-yaml` · `/json/to-xml` · `/json/from-xml`  
> **预估工时**：3h  
> **依赖**：J-03（公共框架），PapaParse CDN，js-yaml CDN，fast-xml-parser CDN

---

## 1. 转换工具公共约定

- 输入区：左侧源格式 Monaco Editor（语言高亮对应格式）
- 输出区：右侧目标格式 Monaco Editor（只读）
- 全部前端完成，无后端
- 支持上传源格式文件，支持下载目标格式文件

---

## 2. JS 实现

### JSON → CSV（`/static/js/json-tool-to-csv.js`）

```javascript
// 依赖 PapaParse
function processJson() {
  const parsed = parseInput();
  if (parsed === null) return;
  if (!Array.isArray(parsed)) { showToast('输入必须为 JSON 数组', 'error'); return; }

  const csv = Papa.unparse(parsed, {
    quotes:    true,
    delimiter: document.getElementById('csvDelimiter')?.value || ',',
    header:    true,
  });
  setOutput(csv, 'plaintext');
  document.getElementById('downloadBtn').onclick = () =>
    downloadOutput('output.csv', 'csv');
}
```

### CSV → JSON（`/static/js/json-tool-from-csv.js`）

```javascript
function processJson() {
  const raw = getInput().trim();
  if (!raw) return;

  const result = Papa.parse(raw, {
    header:        true,
    dynamicTyping: document.getElementById('dynamicTyping')?.checked !== false,
    skipEmptyLines:true,
  });

  if (result.errors.length > 0) {
    showToast('CSV 解析错误：' + result.errors[0].message, 'error');
  }
  setOutput(JSON.stringify(result.data, null, 2));
}
```

### JSON → YAML（`/static/js/json-tool-to-yaml.js`）

```javascript
// 依赖 js-yaml
function processJson() {
  const parsed = parseInput();
  if (parsed === null) return;
  try {
    const yaml = jsyaml.dump(parsed, { indent: 2, lineWidth: 120 });
    setOutput(yaml, 'yaml');
    downloadOutput('output.yaml', 'yaml');
  } catch(e) {
    showToast('YAML 转换失败：' + e.message, 'error');
  }
}
```

### YAML → JSON（`/static/js/json-tool-from-yaml.js`）

```javascript
function processJson() {
  const raw = getInput().trim();
  if (!raw) return;
  try {
    const parsed = jsyaml.load(raw);
    setOutput(JSON.stringify(parsed, null, 2));
  } catch(e) {
    showToast('YAML 解析错误：' + e.message, 'error');
  }
}
```

### JSON → XML（`/static/js/json-tool-to-xml.js`）

```javascript
function processJson() {
  const parsed = parseInput();
  if (parsed === null) return;
  const rootName = document.getElementById('xmlRoot')?.value || 'root';

  function jsonToXml(obj, name) {
    if (Array.isArray(obj)) {
      return obj.map(item => jsonToXml(item, name.replace(/s$/, ''))).join('\n');
    }
    if (typeof obj !== 'object' || obj === null) {
      return `<${name}>${escapeXml(String(obj))}</${name}>`;
    }
    const inner = Object.entries(obj)
      .map(([k, v]) => jsonToXml(v, k))
      .join('\n  ');
    return `<${name}>\n  ${inner}\n</${name}>`;
  }

  const xml = `<?xml version="1.0" encoding="UTF-8"?>\n${jsonToXml(parsed, rootName)}`;
  setOutput(xml, 'xml');
}

function escapeXml(s) {
  return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')
           .replace(/"/g,'&quot;').replace(/'/g,'&apos;');
}
```

### XML → JSON（`/static/js/json-tool-from-xml.js`）

```javascript
// 依赖 fast-xml-parser
function processJson() {
  const raw = getInput().trim();
  if (!raw) return;
  try {
    const { XMLParser } = window.fxparser || window;
    const parser = new XMLParser({ ignoreAttributes: false, attributeNamePrefix: '@_' });
    const result = parser.parse(raw);
    setOutput(JSON.stringify(result, null, 2));
  } catch(e) {
    showToast('XML 解析错误：' + e.message, 'error');
  }
}
```

---

## 3. 验收标准

- [ ] JSON→CSV：`[{"name":"Alice","age":30}]` 输出 `"name","age"\n"Alice",30`
- [ ] CSV→JSON：上传 .csv 文件正确解析，`dynamicTyping` 数字自动转 number
- [ ] JSON→YAML：嵌套对象正确缩进
- [ ] YAML→JSON：标准 YAML 解析无错误
- [ ] JSON→XML：含 rootName 选项，数组正确拆分为子节点
- [ ] XML→JSON：简单 XML 正确转换

---

---