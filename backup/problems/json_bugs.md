# 问题记录
## Json 工具模块

### bug
1、json 转yaml文件报错，错误提示：js-yaml库未加载，请刷新页面重试。yaml转json同样的错误也需要修改
2、JSON Path 查询，输入json字符串报错，错误提示“请输入JSONPath 表达式”
3、json转csv有bug，点执行后，右边输入框无结果
4、csv转json有bug，点执行后，右边输入框无结果
5、XML 转 JSON有bug，点执行后提示“XML 解析库未加载，请刷新页面重试”
6、JSON 转 Markdown有bug，结果不符合预期。
7、JSON Schema 验证有bug，现在点执行提示“”，结果不符合预期，另外，为了提升体验，执行按钮放在两个输入框中间。
